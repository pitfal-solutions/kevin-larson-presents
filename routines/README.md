# Routines

Recurring-task definitions for this workspace. Documentation only until
explicitly scheduled — writing a routine here does not make it run
automatically. Ask before wiring one up to a real schedule.

Nothing scheduled yet for this project. Once the client engages past the
demo (real site build, ongoing marketing), candidates for a routine here
could include: a periodic check that all 5 event dates/pricing links are
still current, or a periodic social-media content digest if the marketing
upsell happens.
